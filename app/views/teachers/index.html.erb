<h1>Teachers</h1>

<ul>
    <% @teachers.each do |teacher| %>
        <li>
            <%=link_to(teacher.name, teacher) %>
        </li>
    <% end %>
</ul>
<%= button_to("Create new Teacher", new_teacher_path, method: "get")%>